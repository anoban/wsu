{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "StaticBuild",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}/OpenSimRoot/StaticBuild"
            },
            "command": "make -j4 all"
        }
    ]
}